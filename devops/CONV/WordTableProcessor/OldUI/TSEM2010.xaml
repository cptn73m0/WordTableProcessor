<UserControl x:Class="WordTableProcessor.TSEM2010"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="SelectWordButton" Content="Выбрать Word документ" Click="SelectWordButton_Click" Width="150" Margin="0,0,10,0"/>
            <TextBlock x:Name="WordFilePathTextBlock" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="SelectCsvButton" Content="Выбрать CSV файл" Click="SelectCsvButton_Click" Width="150" Margin="0,0,10,0"/>
            <TextBlock x:Name="CsvFilePathTextBlock" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Grid.Row="2" x:Name="CleanTableButton" Content="Очистить таблицу" Click="CleanTableButton_Click" Width="150" HorizontalAlignment="Left" Margin="0,0,0,10"/>
        <Button Grid.Row="3" x:Name="UpdatePricesButton" Content="Обновить цены" Click="UpdatePricesButton_Click" Width="150" HorizontalAlignment="Left" Margin="0,0,0,10"/>
        <TextBox Grid.Row="4" x:Name="StatusTextBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>
        <ProgressBar Grid.Row="5" x:Name="ProgressBar" Height="20" Minimum="0" Maximum="100" Value="0"/>
    </Grid>
</UserControl>