<UserControl x:Class="WordTableProcessor.Views.DocumentProcessorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WordTableProcessor"
             xmlns:controls="clr-namespace:WordTableProcessor.Controls"
             xmlns:models="clr-namespace:WordTableProcessor.Models"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#E5E5E5"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="BorderBrush" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2" Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.3" Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="2" Opacity="0.15" Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
        </Style>

        <Style x:Key="DocumentTypeComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#AAAAAA"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Обработка документов" FontSize="20" FontWeight="Bold" 
                   Margin="0,0,0,20" Foreground="#1A1A1A"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Тип документа:" VerticalAlignment="Center" 
                       Margin="0,0,15,0" FontWeight="SemiBold" Foreground="#1A1A1A"/>
            <ComboBox x:Name="DocumentTypeComboBox" Grid.Column="1"
                      Style="{StaticResource DocumentTypeComboBoxStyle}"
                      SelectionChanged="DocumentTypeComboBox_SelectionChanged"
                      MaxWidth="400" HorizontalAlignment="Left"/>
        </Grid>

        <controls:FileDropZone x:Name="DropZone" Grid.Row="2" Margin="0,0,0,20"
                               FileDropped="DropZone_FileDropped"
                               FileSelected="DropZone_FileSelected"/>

        <GroupBox Grid.Row="3" Header="Выбранные файлы" Margin="0,0,0,20"
                  Padding="15" BorderBrush="#D1D1D1">
            <StackPanel>
                <StackPanel x:Name="WordFilePanel" Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol" FontSize="16" 
                               VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#0078D4"/>
                    <TextBlock x:Name="WordFileName" Text="Word документ не выбран" 
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#1A1A1A"/>
                    <Button x:Name="ClearWordButton" Content="&#xE711;" 
                            FontFamily="Segoe MDL2 Assets, Segoe UI Symbol" Width="28" Height="28"
                            Background="Transparent" BorderThickness="0" Margin="10,0,0,0"
                            Cursor="Hand" Click="ClearWordButton_Click"
                            ToolTip="Удалить"/>
                </StackPanel>
                <StackPanel x:Name="CsvFilePanel" Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="&#xE80A;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol" FontSize="16" 
                               VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#0078D4"/>
                    <TextBlock x:Name="CsvFileName" Text="CSV файл не выбран" 
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#1A1A1A"/>
                    <Button x:Name="ClearCsvButton" Content="&#xE711;" 
                            FontFamily="Segoe MDL2 Assets, Segoe UI Symbol" Width="28" Height="28"
                            Background="Transparent" BorderThickness="0" Margin="10,0,0,0"
                            Cursor="Hand" Click="ClearCsvButton_Click"
                            ToolTip="Удалить"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="CleanTableButton" Content="Очистить таблицу" 
                    Style="{StaticResource ActionButtonStyle}"
                    Click="CleanTableButton_Click"/>
            <Button x:Name="UpdatePricesButton" Content="Обновить цены" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="UpdatePricesButton_Click"/>
        </StackPanel>

        <controls:ProgressPanel x:Name="ProgressControl" Grid.Row="5" MinHeight="100"/>
    </Grid>
</UserControl>
