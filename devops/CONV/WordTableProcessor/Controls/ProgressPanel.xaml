<UserControl x:Class="WordTableProcessor.Controls.ProgressPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#E5E5E5"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#F5F5F5" BorderBrush="#D1D1D1" BorderThickness="1" CornerRadius="6" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusBlock" Grid.Column="0" Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="SemiBold" Foreground="#1A1A1A"/>
                <TextBlock x:Name="PercentBlock" Grid.Column="1" Text="{Binding PercentText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#666666"/>
            </Grid>

            <ProgressBar x:Name="ProgressBar" Grid.Row="1" 
                         Value="{Binding Progress, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Maximum="100" Minimum="0"
                         Style="{StaticResource ProgressBarStyle}"
                         Margin="0,0,0,8"/>

            <TextBox x:Name="LogTextBox" Grid.Row="2"
                     Text="{Binding LogText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                     IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="Transparent"
                     BorderThickness="0"
                     FontFamily="Consolas" FontSize="11"
                     Foreground="#666666"/>
        </Grid>
    </Border>
</UserControl>
